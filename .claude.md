# img-processor

图片处理器 - 一个用于将 JPG 图片批量生成 PDF 文档的工具。

## 项目简介

本项目提供了一套图片处理工具，主要功能包括：

1. **PDF 生成器**：将指定目录（包括子目录）中的 JPG 图片合并生成 PDF 文档
2. **生产文件夹生成器**：从源目录中筛选以 "1" 开头的 JPG 文件，复制到新的生产目录

## 技术栈

- **运行时**: Node.js (ES Modules)
- **包管理**: pnpm
- **核心依赖**:
  - `pdfkit` - PDF 文档生成
  - `sharp` - 图片压缩处理
  - `woff2-encoder` - 字体文件解压（支持中文字体）
  - `inquirer` - 命令行交互界面
- **构建工具**: `pkg` - 打包为可执行文件

## 项目结构

```
img-processor/
├── index.js              # CLI 入口（交互式命令行模式）
├── server.js             # Web 服务入口（HTTP 服务模式）
├── package.json          # 项目配置
├── src/
│   ├── pdf-generator.js       # PDF 生成核心模块
│   ├── prod-folder-generator.js # 生产文件夹生成模块
│   └── fonts/
│       └── SourceHanSans.woff2  # 思源黑体字体（支持中文）
├── public/
│   ├── index.html        # Web 界面
│   └── style.css         # 样式文件
└── plugin/               # Chrome 浏览器扩展
    ├── manifest.json     # 扩展配置
    ├── background.js     # 扩展后台脚本
    ├── content.js        # 内容脚本
    ├── popup.html/js     # 弹出窗口
    └── native_host.py    # 原生消息主机（用于目录选择）
```

## 运行方式

### 命令行模式（CLI）

```bash
pnpm start
# 或
node index.js
```

交互式界面，支持：
- 选择操作类型（生成 PDF / 生成生产文件夹）
- 通过文件选择器选择目标目录

### Web 服务模式

```bash
pnpm web
# 或
node server.js
```

启动 HTTP 服务器，监听端口 `3000`，访问 `http://localhost:3000` 使用 Web 界面。

### API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/run` | 生成 PDF，参数：`folderPath`（目录路径） |
| POST | `/prod-folder` | 生成生产文件夹，参数：`folderPath`（目录路径） |
| GET | `/*` | 静态文件服务（`public/` 目录） |

## 构建打包

```bash
pnpm build
```

使用 `pkg` 打包为独立可执行文件 `img-processor`，目标为 Node.js 18。

## Chrome 扩展

`plugin/` 目录包含一个 Chrome 浏览器扩展，用于：
- 通过原生消息（Native Messaging）调用系统目录选择对话框
- 自动填充目录路径到 Web 界面

### 扩展安装

1. 安装 Python 3（用于原生主机脚本）
2. 配置原生消息主机：将 `com.lebang.img_processor.json` 复制到:
   - macOS: `~/Library/Application Support/Google/Chrome/NativeMessagingHosts/`
3. 在 Chrome 中加载 `plugin/` 目录作为未打包的扩展

## 核心功能说明

### PDF 生成流程

1. 递归扫描目标目录中的所有 JPG/JPEG 文件
2. 按文件名自然排序
3. 对每张图片进行 JPEG 压缩（质量 75%）
4. 使用思源黑体字体显示目录名作为页面标题
5. 生成 PDF 文件，保存在源目录中

### 生产文件夹生成

- 筛选文件名以 "1" 开头且扩展名为 `.jpg` 的文件
- 保持原目录结构复制到 `{源目录名}-prod` 文件夹

## 开发注意事项

- 项目使用 ES Modules (`"type": "module"`)
- 字体文件 `SourceHanSans.woff2` 是必需的，用于 PDF 中文显示
- Web 服务包含路径安全检查，防止目录遍历攻击
- 图片压缩可调整 `sharp` 的 `quality` 参数控制输出质量
